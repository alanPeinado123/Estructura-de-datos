// implementacion en JavaScript
const arr = [1, 2, 3, 4, 5];

console.log("Los elementos del array son:");
for (const element of arr) {
    process.stdout.write(element + " "); // mostrando los elementos del array separados por espacios especiales
}
