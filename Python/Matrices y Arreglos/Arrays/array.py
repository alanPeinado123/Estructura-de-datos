import array as arr
arr = arr.array('i', [1, 2, 3, 4, 5])
print("Los elementos del array son:")   
for element in arr:
    print(element, end=" ") # mostrando los elementos del array separados por espacios especiales